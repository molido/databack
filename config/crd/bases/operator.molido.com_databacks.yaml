---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: databacks.operator.molido.com
spec:
  group: operator.molido.com
  names:
    kind: Databack
    listKind: DatabackList
    plural: databacks
    singular: databack
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: Databack is the Schema for the databacks API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Databack
            properties:
              destination:
                description: 备份目标地址
                properties:
                  accessKey:
                    type: string
                  accessSecret:
                    type: string
                  buketName:
                    type: string
                  endpoint:
                    type: string
                required:
                - accessKey
                - accessSecret
                - buketName
                - endpoint
                type: object
              enable:
                description: 是否开启备份任务
                type: boolean
              origin:
                description: 数据源
                properties:
                  host:
                    description: 数据库访问地址
                    type: string
                  password:
                    type: string
                  port:
                    description: 数据库端口
                    format: int32
                    type: integer
                  username:
                    type: string
                required:
                - host
                - password
                - port
                - username
                type: object
              period:
                description: 间隔周期（分钟）
                type: integer
              startTime:
                description: 数据备份开始备份时间 12:00
                type: string
            required:
            - destination
            - enable
            - origin
            - period
            - startTime
            type: object
          status:
            description: status defines the observed state of Databack
            properties:
              active:
                type: boolean
              lastBackupResult:
                type: string
              nextTime:
                format: int64
                type: integer
            required:
            - active
            - lastBackupResult
            - nextTime
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
